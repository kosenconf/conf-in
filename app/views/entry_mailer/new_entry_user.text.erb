<%= @user.name %> 様
下記のイベントへの参加登録を完了しました。

「<%= @event.name %>」
<%= event_url(@event) %>

開催日時： <%= I18n.l @event.date, format: :long %>
会場： <%= @event.place_name %>
参加費用： <% @entry.entry_fees.each do |fee| %>
	<%= fee.event_fee.name %>: <%= number_to_currency(fee.event_fee.sum, unit: "円", precision: 0, format: "%n %u") %><% end %>
	合計： <%= number_to_currency(@entry.fees_sum, unit: "円", precision: 0, format: "%n %u") %>
連絡先： <%= @event.hosting_email %>

<% if @event.qr_use %>
***************************************************************************************************
【当日受付用QRコード】

 ※当日はこのメールに「添付」されている「QRコード」を
 	「印刷する」または「モバイル端末の画面に表示する」などして，
 提示可能な状態で忘れずにお持ちください。
***************************************************************************************************
<% end %>

--
カンファイン
<%= root_url %>
システムに関してのお問い合わせは，
<%= raw "#{help_url}#contact" %>
までお願いいたします。
※イベントに関するお問い合わせは，イベント主催者にお願いいたします。
